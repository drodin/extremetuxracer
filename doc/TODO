
- Compilation to work when clang++ is in use
- Font with all characters required by supported translations
- MinGW build support
- Handle errors caused by corrupted user configuration files
  - Existing 'players' file without valid players crash only when
    screen refresh needs player information.